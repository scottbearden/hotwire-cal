<div><%= calendar.name %> - <%= calendar.color %></div>
<div><%= link_to calendar.name, calendar %> - <%= calendar.color %></div>
<div class="mt-2">
  <%= link_to "Edit", edit_calendar_path(calendar), class: "text-blue-600 hover:text-blue-800" %>
  <%= button_to "Destroy this calendar", calendar, method: :delete, class: "text-red-600 hover:text-red-800", form: { style: "display: inline;" } %>
</div>
